# Bulk edit / delete association values prototype

Run the prototype with:

```sh
npm start
```

Then open <http://127.0.0.1:4173>.

The five Figma screens are connected through the product, association, expansion, selection, and course-detail interactions. The add, edit, delete, and detach modal flows update local in-memory state and show confirmation feedback.
